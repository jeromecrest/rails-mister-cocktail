<div class="container-form">

  <%= simple_form_for(cocktail) do |f| %>
    <%= f.input :name   %>
    <%= f.input :photo, as: :file %>
    <%= f.submit "Add this cocktail", class: "btn btn-secondary" %>

  <% end %>

  <%= link_to "See all cocktails", cocktails_path  %>

</div>
